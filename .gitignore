dist
node_modules
.DS_Store

# SQLite persistence (query history + saved queries) — DL-013
# Root-anchored so it ignores only the repo-root DB dir, not web/src/data/ (golden dataset).
/data/
*.db
*.db-shm
*.db-wal
*.sqlite

# Secrets — server-side API keys (e.g. ANTHROPIC_API_KEY for the AI assistant, DL-031)
.env
.env.*
!.env.example
